{
  "buildCommand": "npm install -g pnpm@9 && cd book && pnpm install && pnpm build",
  "outputDirectory": "book/build",
  "installCommand": "echo skip",
  "framework": null,
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
