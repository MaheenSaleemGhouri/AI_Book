{
  "buildCommand": "cd book && pnpm install --frozen-lockfile && pnpm build",
  "outputDirectory": "book/build",
  "installCommand": "echo skip",
  "framework": null,
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
