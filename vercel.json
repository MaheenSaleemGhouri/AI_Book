{
  "buildCommand": "cd book && npm install --legacy-peer-deps && npm run build",
  "outputDirectory": "book/build",
  "installCommand": "echo skip",
  "framework": null,
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
